---
title: CI/CD Secrets
description: Use dotsecenv secrets in GitHub Actions and other CI/CD pipelines
---

import { Steps, Aside } from '@astrojs/starlight/components';

<Aside type="tip" title="Runnable example">
A complete `.github/workflows/deploy.yml` plus a bootstrap walkthrough (CI key generation, repo-secret setup, recipient grant) ships at [`examples/03-ci-cd-github-action/`](https://github.com/dotsecenv/dotsecenv/tree/main/examples/03-ci-cd-github-action) on the CLI repo.
</Aside>

## Goal

Access dotsecenv-managed secrets in a GitHub Actions workflow so your CI/CD pipeline can use them without storing plaintext credentials in your repo.

## Prerequisites

- [Getting Started](/getting-started/) completed
- A GitHub repository with Actions enabled
- A GPG key dedicated to CI (recommended: `dotsecenv identity create --name "CI Bot" --email "ci@example.com" --no-passphrase`)

## Steps

<Steps>

1. **Export your CI GPG private key**

   Export the ASCII-armored private key directly. Do not base64-encode it; the armor format is already plain ASCII and `gpg --import` reads it as-is.

   ```bash
   gpg --armor --export-secret-keys ci@example.com > ci-key.asc
   ```

   <Aside type="caution">
   Use a dedicated CI key, not your personal key. This key will be stored as a GitHub secret. Delete `ci-key.asc` once it is uploaded.
   </Aside>

2. **Add it as a GitHub secret**

   Go to your repository: **Settings > Secrets and variables > Actions > New repository secret**

   - Name: `GPG_PRIVATE_KEY`
   - Value: paste the contents of `ci-key.asc`

   Or use the `gh` CLI:

   ```bash
   gh secret set GPG_PRIVATE_KEY < ci-key.asc
   ```

   <Aside type="note" title="Repo-scoped vs org-wide keypair">
   This tutorial stores the key as a **repository** secret, so one CI keypair serves one repo. To share a single keypair across many repos in an organization, store it as an organization secret named `ORG_GPG_PRIVATE_KEY` instead. See [Key Scope](/concepts/key-scope/) for the trade-offs and [Org-wide Keypair](/tutorials/org-wide-keypair/) for the setup.
   </Aside>

3. **Create a workflow file**

   Create `.github/workflows/build.yml`:

   ```yaml
   name: Build

   on:
     push:
       branches: [main]

   permissions:
     contents: read

   jobs:
     build:
       runs-on: ubuntu-latest
       env:
         GNUPGHOME: ${{ runner.temp }}/gnupg
       steps:
         - uses: actions/checkout@v4

         - uses: dotsecenv/dotsecenv@v0
           with:
             version: latest
             verify-provenance: true

         - name: Import GPG key
           env:
             GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
           run: |
             mkdir -p "$GNUPGHOME"
             chmod 700 "$GNUPGHOME"
             echo "$GPG_PRIVATE_KEY" | gpg --batch --import
             # Capture the fingerprint of the imported key for login.
             FP=$(gpg --list-secret-keys --with-colons \
               | awk -F: '/^fpr:/ { print $10; exit }')
             echo "DOTSECENV_FP=$FP" >> "$GITHUB_ENV"

         - name: Initialise dotsecenv and log in
           run: |
             dotsecenv init config -v .dotsecenv/vault
             dotsecenv login "$DOTSECENV_FP"

         - name: Use secrets
           run: |
             DATABASE_PASSWORD=$(dotsecenv secret get DATABASE_PASSWORD)
             echo "Secret retrieved successfully"
             # Use $DATABASE_PASSWORD in your build/deploy steps
   ```

4. **Commit and push**

   ```bash
   git add .github/workflows/build.yml
   git commit -m "ci: add dotsecenv secrets to build workflow"
   git push
   ```

5. **Verify the workflow**

   Go to **Actions** in your repository. The workflow should complete successfully with "Secret retrieved successfully" in the logs.

   <Aside type="tip">
   The actual secret value never appears in logs. GitHub Actions automatically masks secrets set via the `secrets` context, and `dotsecenv secret get` writes to stdout only.
   </Aside>

</Steps>

## Using `.secenv` files in CI

If your project has a `.secenv` file, you can load all secrets at once:

```yaml
     - name: Load secrets from .secenv
       run: |
         eval "$(dotsecenv env load)"
         # All .secenv variables are now available
```

## GitLab CI

The pattern is the same. Store the ASCII-armored GPG key as a CI/CD variable named `GPG_PRIVATE_KEY` (type "File" or "Variable"; do not base64-encode it), then:

```yaml
build:
  image: ubuntu:latest
  before_script:
    - curl -fsSL https://get.dotsecenv.com/install.sh | bash -s -- --no-install-shell-plugin
    - echo "$GPG_PRIVATE_KEY" | gpg --batch --import
    - dotsecenv init config -v .dotsecenv/vault
    - FP=$(gpg --list-secret-keys --with-colons | awk -F: '/^fpr:/ { print $10; exit }')
    - dotsecenv login "$FP"
  script:
    - DATABASE_PASSWORD=$(dotsecenv secret get DATABASE_PASSWORD)
    - echo "Secret available in pipeline"
```

## Next steps

- [GitHub Action](/guides/github-action/) -- full reference for the `dotsecenv/dotsecenv` action
- [Terraform & OpenTofu](/guides/terraform-credentials-helper/) -- use dotsecenv as a Terraform credentials helper
