name: Lint workflows

# actionlint catches workflow syntax errors, broken `uses:` refs (local
# or remote), dangling `needs:`, undeclared inputs, invalid expressions,
# bad shell, and many other issues at PR-time. This is the only check
# that validates release.yml and post-release-action.yml on PR — both
# are tag-triggered or workflow_call-only and otherwise never fire
# pre-merge.

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/**"
      - ".github/actionlint*"
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/**"
      - ".github/actionlint*"

permissions:
  contents: read

jobs:
  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run actionlint
        uses: docker://rhysd/actionlint:1.7.7
